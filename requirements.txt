# ==========================================================
# PrognosX — requirements.txt
# NOTE: This file currently covers Phases 1-3
# (Project Foundation, Dataset Acquisition, FD001 Exploration).
# Modeling / DL / survival / API / deployment libraries will be
# added incrementally as later phases are implemented.
# ==========================================================

# --- Core data processing ---
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=15.0.0   # required by pandas for .parquet read/write (ingestion.py)

# --- Visualization (EDA / sensor analysis) ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Notebooks ---
jupyter>=1.0.0
ipykernel>=6.29.0
nbformat>=5.10.0

# --- Config / env ---
PyYAML>=6.0.1
python-dotenv>=1.0.1

# --- Logging / utils ---
rich>=13.7.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0

# --- Code quality ---
black>=24.1.0
flake8>=7.0.0
